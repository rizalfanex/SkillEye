// Template for secrets.h -- copy this file to secrets.h and edit it.
//
//     cp secrets.example.h secrets.h
//
// secrets.h is gitignored so the device's WiFi credentials never land in the
// repo. This file (the template) is tracked, so keep placeholders in it.
//
// The password must be at least 8 characters or WPA2 refuses it and the AP
// comes up open.

#pragma once

#define AP_SSID      "skilleye"
#define AP_PASSWORD  "CHANGE_ME"
#define TCP_PORT     3333
